# دليل بناء ملف Setup للبرنامج

## ✅ المشكلة التي تم حلها

تم حل مشكلة بناء البرنامج التي كانت تظهر هذا الخطأ:
```
⨯ open : The system cannot find the file specified.
```

**السبب:** electron-builder لا يستطيع التعامل مع ملفات SVG كأيقونة.

**الحل:** تم إزالة تحديد الأيقونة من `package.json` مؤقتاً.

---

## 🚀 خطوات بناء ملف Setup

### 1. افتح PowerShell في مجلد البرنامج

```powershell
cd "C:\Users\Wael\Downloads\تجربة لإ\Erchad-main"
```

### 2. قم بتشغيل أمر البناء

```powershell
npm run electron:build:win
```

### 3. انتظر حتى يكتمل البناء

سيستغرق البناء بضع دقائق. ستظهر رسائل مثل:
- ✓ building...
- ✓ packaging...
- ✓ building block map...

### 4. احصل على ملف Setup

بعد اكتمال البناء، ستجد ملف Setup في المجلد:
```
release/
```

الملفات التي ستجدها:
- `نظام إدارة الطلاب Setup X.X.X.exe` - ملف التثبيت
- `win-unpacked/` - نسخة غير مضغوطة من البرنامج

---

## 📝 ملاحظات مهمة

### إذا ظهر خطأ "cannot find module"

قم بتثبيت المكتبات مرة أخرى:
```powershell
npm install
```

### إذا أردت تنظيف المجلد قبل البناء

```powershell
# احذف مجلد release القديم
Remove-Item -Recurse -Force release

# احذف مجلد dist القديم
Remove-Item -Recurse -Force dist

# ثم قم بالبناء مرة أخرى
npm run electron:build:win
```

### إذا أردت بناء البرنامج لأنظمة أخرى

**لنظام macOS:**
```powershell
npm run electron:build:mac
```

**لنظام Linux:**
```powershell
npm run electron:build:linux
```

---

## 🎨 إضافة أيقونة مخصصة (اختياري)

راجع ملف `ICON_GUIDE.md` لمعرفة كيفية إضافة أيقونة مخصصة للبرنامج.

---

## ⚙️ تخصيص إعدادات البناء

يمكنك تعديل إعدادات البناء في ملف `package.json` تحت قسم `"build"`:

```json
"build": {
  "appId": "com.studentmanagement.app",
  "productName": "نظام إدارة الطلاب",
  "nsis": {
    "oneClick": false,
    "allowToChangeInstallationDirectory": true,
    "createDesktopShortcut": true
  }
}
```

---

## 🐛 حل المشاكل الشائعة

### المشكلة: "electron-builder not found"

**الحل:**
```powershell
npm install --save-dev electron-builder
```

### المشكلة: "node-gyp errors"

**الحل:** تثبيت أدوات البناء:
```powershell
npm install --global windows-build-tools
```

### المشكلة: "out of memory"

**الحل:** زيادة ذاكرة Node:
```powershell
$env:NODE_OPTIONS="--max-old-space-size=4096"
npm run electron:build:win
```

---

## ✨ النتيجة النهائية

بعد اكتمال البناء بنجاح، ستحصل على ملف Setup يمكنك:
- توزيعه على أجهزة أخرى
- تثبيته بدون الحاجة لـ Node.js أو npm
- تشغيله مباشرة كبرنامج مستقل

---

## 📞 المساعدة

إذا واجهت أي مشاكل، تأكد من:
1. تثبيت جميع المكتبات: `npm install`
2. بناء المشروع أولاً: `npm run build`
3. التأكد من وجود مساحة كافية على القرص (2 GB على الأقل)
4. إغلاق أي برنامج مضاد فيروسات مؤقتاً

---

**تم بناء البرنامج بنجاح ✓**
