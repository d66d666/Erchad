# دليل البدء السريع - حل مشكلة تسجيل الدخول

## ✅ المشكلة: لا يمكن الكتابة في حقول تسجيل الدخول

تم إصلاح المشكلة بالكامل! التحديثات التي تمت:

### 1. تحديث إعدادات Electron
- إضافة `sandbox: false` لتمكين الكتابة في الحقول
- إضافة `spellcheck: false` لتعطيل التدقيق الإملائي
- إضافة `show: false` و `ready-to-show` لتحسين الأداء

### 2. تحسين حقول النموذج
- إضافة `name` و `id` لكل حقل
- إضافة `autoComplete="off"` لمنع الاقتراحات التلقائية
- إضافة `autoFocus` لحقل اسم المستخدم للتركيز التلقائي
- إضافة `tabIndex={-1}` لزر إظهار/إخفاء كلمة المرور

---

## 🚀 خطوات تشغيل البرنامج (بعد التحديثات)

### الطريقة 1: تشغيل في وضع التطوير (للاختبار)

```powershell
# 1. افتح PowerShell في مجلد البرنامج
cd "C:\Users\Wael\Downloads\تجربة لإ\Erchad-main"

# 2. تثبيت المكتبات (مرة واحدة فقط)
npm install

# 3. تشغيل البرنامج في وضع التطوير
npm run electron:dev
```

**ملاحظة:** هذه الطريقة للاختبار فقط. البرنامج سيفتح نافذتين: متصفح + نافذة Electron.

---

### الطريقة 2: بناء ملف Setup (الطريقة الصحيحة)

```powershell
# 1. افتح PowerShell في مجلد البرنامج
cd "C:\Users\Wael\Downloads\تجربة لإ\Erchad-main"

# 2. تأكد من تثبيت المكتبات
npm install

# 3. بناء ملف Setup
npm run electron:build:win
```

**ملاحظة:** قد يستغرق البناء 5-10 دقائق حسب سرعة الجهاز.

بعد انتهاء البناء:
- ستجد ملف Setup في مجلد `release/`
- اسم الملف: `نظام إدارة الطلاب Setup 1.0.0.exe`
- قم بتثبيت البرنامج على جهازك

---

## 🔑 معلومات تسجيل الدخول

### حساب المطور (الحساب الرئيسي)

```
اسم المستخدم: Wael
كلمة المرور: 0558890902
```

**هذا الحساب له صلاحيات كاملة على النظام**

### إنشاء حسابات جديدة

بعد تسجيل الدخول بحساب المطور:
1. اذهب إلى صفحة "إدارة الحسابات"
2. اضغط على "إضافة حساب جديد"
3. أدخل البيانات المطلوبة
4. حدد الصلاحيات والصلاحية

---

## ❓ حل المشاكل الشائعة

### المشكلة: لا يمكن الكتابة في الحقول

**الحل:**
1. تأكد من أنك تستخدم أحدث إصدار من البرنامج
2. أعد بناء البرنامج بأمر `npm run electron:build:win`
3. ثبّت البرنامج من جديد

---

### المشكلة: "npm is not recognized"

**الحل:**
1. تأكد من تثبيت Node.js أولاً
2. حمّل Node.js من: https://nodejs.org
3. اختر "LTS" (النسخة المستقرة)
4. أعد تشغيل PowerShell بعد التثبيت

---

### المشكلة: "cannot find module"

**الحل:**
```powershell
# احذف مجلد node_modules
Remove-Item -Recurse -Force node_modules

# احذف ملف package-lock.json
Remove-Item package-lock.json

# أعد تثبيت المكتبات
npm install
```

---

### المشكلة: البرنامج لا يفتح بعد البناء

**الحل:**
1. تأكد من إكمال البناء بدون أخطاء
2. ابحث عن ملف Setup في مجلد `release/`
3. قم بتشغيل ملف Setup كمسؤول (Run as Administrator)
4. إذا ظهر تحذير من Windows Defender، اختر "Run anyway"

---

### المشكلة: الشاشة فارغة بعد فتح البرنامج

**الحل:**
1. اضغط `Ctrl+R` لإعادة تحميل الصفحة
2. أو أغلق البرنامج وافتحه من جديد
3. إذا استمرت المشكلة، أعد بناء البرنامج

---

## 📋 الميزات الرئيسية

- ✅ إدارة الطلاب (إضافة، تعديل، حذف)
- ✅ استيراد من Excel
- ✅ التصدير إلى Excel
- ✅ إدارة الحضور والغياب
- ✅ إدارة الحالات الخاصة
- ✅ إدارة المجموعات
- ✅ إدارة الحسابات والصلاحيات
- ✅ البحث والتصفية المتقدمة
- ✅ فتح واتساب مباشرة
- ✅ الطباعة
- ✅ نظام التجديد بالأكواد
- ✅ استعادة كلمة المرور

---

## 📞 الدعم الفني

إذا واجهت أي مشكلة:
1. تأكد من قراءة هذا الدليل كاملاً
2. راجع ملف `BUILD_SETUP_GUIDE.md` للمزيد من التفاصيل
3. تواصل مع الدعم الفني

---

**تم التحديث:** 2025-12-06

**الإصدار:** 1.0.0

**الحالة:** ✅ جاهز للاستخدام
