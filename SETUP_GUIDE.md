# 📖 دليل استخدام ملف التصفير (Setup)

## 🎯 نظرة عامة

ملف `setup.html` هو أداة قوية لتصفير قاعدة البيانات وإعادة النظام إلى حالته الأولية.

---

## 🚀 طرق فتح ملف Setup

### ✅ الطريقة الأولى: من المتصفح مباشرة
1. شغّل المشروع: `npm run dev`
2. افتح المتصفح واذهب إلى: `http://localhost:5173/setup.html`

### ✅ الطريقة الثانية: فتح الملف مباشرة
1. اذهب إلى مجلد المشروع
2. افتح ملف `setup.html` بنقرة مزدوجة
3. سيتم فتحه في المتصفح الافتراضي

### ✅ الطريقة الثالثة: من النسخة المبنية
1. بعد البناء: `npm run build`
2. اذهب إلى مجلد `dist`
3. افتح ملف `setup.html`

---

## ⚠️ تحذيرات هامة جداً!

### 🔴 قبل التصفير

#### 1. اعمل نسخة احتياطية!
- افتح النظام
- اذهب إلى "إعداد" (أيقونة المستخدم)
- اضغط على "تصدير البيانات الكاملة"
- احفظ ملف JSON في مكان آمن

#### 2. أغلق جميع نوافذ النظام
- تأكد من إغلاق جميع نوافذ التطبيق الأخرى
- اترك نافذة setup.html فقط

#### 3. فهم المخاطر
- ✅ الحذف نهائي ولا يمكن التراجع عنه
- ✅ جميع البيانات سيتم حذفها
- ✅ لا توجد طريقة للاسترجاع بدون نسخة احتياطية

---

## 📋 البيانات التي سيتم حذفها

عند الضغط على زر "تصفير قاعدة البيانات"، سيتم حذف:

| البيانات | التفاصيل |
|---------|----------|
| 👨‍🎓 **الطلاب** | جميع بيانات الطلاب والسجلات |
| 👨‍🏫 **المعلمين** | جميع المعلمين وربطهم بالمجموعات |
| 📚 **المجموعات** | جميع المجموعات والصفوف |
| 🏷️ **فئات الطلاب** | جميع الفئات والحالات الخاصة |
| 🚶 **الزيارات** | جميع سجلات الزيارات |
| 📝 **الاستئذانات** | جميع طلبات الاستئذان |
| ⚠️ **المخالفات** | جميع المخالفات المسجلة |
| 👤 **الحسابات** | جميع حسابات المستخدمين |
| ⚙️ **الإعدادات** | جميع الإعدادات والتفضيلات |
| 📅 **الاشتراكات** | بيانات الاشتراك والترخيص |

---

## 📝 خطوات التصفير بالتفصيل

### 1️⃣ افتح صفحة Setup
- اتبع إحدى الطرق المذكورة أعلاه

### 2️⃣ اقرأ التحذيرات
- **مربع أحمر**: تحذيرات رئيسية
- **مربع أزرق**: قائمة البيانات التي سيتم حذفها

### 3️⃣ اضغط على زر "تصفير قاعدة البيانات"
- الزر باللون الأحمر في الأسفل

### 4️⃣ التأكيد الأول
سيظهر تنبيه:
```
⚠️ هل أنت متأكد من تصفير قاعدة البيانات؟

سيتم حذف جميع البيانات بشكل نهائي!
```
- اضغط **OK** للمتابعة
- اضغط **Cancel** للإلغاء

### 5️⃣ التأكيد الثاني
سيظهر تنبيه آخر:
```
⚠️ تأكيد نهائي!

هذا الإجراء لا يمكن التراجع عنه.

هل تريد المتابعة؟
```
- اضغط **OK** للمتابعة النهائية
- اضغط **Cancel** للإلغاء

### 6️⃣ انتظر اكتمال العملية

سيتم عرض شريط تقدم مع النسب التالية:

```
[20%] جاري البحث عن قاعدة البيانات...
[40%] جاري حذف قاعدة البيانات...
[60%] جاري تنظيف البيانات المحلية...
[80%] جاري حذف الإعدادات...
[100%] تم التصفير بنجاح!
```

### 7️⃣ النجاح
- سيظهر مربع أخضر: "✅ تم التصفير بنجاح!"
- سيتم إعادة توجيهك تلقائياً إلى الصفحة الرئيسية بعد ثانيتين

---

## 🔧 حل المشاكل الشائعة

### ❌ مشكلة: "يرجى إغلاق جميع نوافذ النظام الأخرى"

**الحل:**
1. أغلق جميع نوافذ النظام
2. أغلق المتصفح بالكامل
3. افتح المتصفح مرة أخرى
4. افتح `setup.html` فقط
5. حاول مرة أخرى

### ❌ مشكلة: حدث خطأ أثناء التصفير

**الحل:**
1. أعد تحميل الصفحة (F5)
2. تأكد من إغلاق جميع نوافذ النظام
3. جرّب في وضع التصفح الخفي
4. جرّب متصفح آخر
5. أعد تشغيل الحاسوب

### ❌ مشكلة: الصفحة لا تفتح

**الحل:**
1. تأكد من تشغيل المشروع: `npm run dev`
2. تأكد من وجود ملف `setup.html` في المجلد الرئيسي
3. جرّب فتح الملف مباشرة بالنقر المزدوج

---

## 🎯 بعد التصفير - خطوات مهمة

### 1️⃣ تسجيل الدخول الأولي

عند فتح النظام لأول مرة بعد التصفير:

```
اسم المستخدم: admin
كلمة المرور: admin
```

### 2️⃣ تغيير بيانات الدخول (مهم جداً!)

**خطوات التغيير:**
1. اضغط على أيقونة المستخدم (أعلى يسار الشاشة)
2. اضغط على "تغيير بيانات الدخول"
3. أدخل:
   - **اسم المستخدم الجديد**
   - **كلمة المرور الجديدة** (8 أحرف على الأقل)
   - **تأكيد كلمة المرور**
4. اضغط "حفظ التغييرات"
5. سيتم تسجيل خروجك تلقائياً
6. سجّل الدخول بالبيانات الجديدة

### 3️⃣ إعداد معلومات النظام

1. اضغط على أيقونة المستخدم
2. أدخل:
   - **اسم مسؤول النظام**
   - **رقم الجوال**
   - **وصف النظام**
   - **اسم المدرسة**
3. اضغط "حفظ الإعدادات"

### 4️⃣ إضافة البيانات الأساسية

#### أ. إضافة المجموعات
1. اذهب إلى "إدارة المجموعات"
2. اضغط "إضافة مجموعة"
3. أدخل:
   - اسم المجموعة
   - المرحلة
   - ترتيب العرض
4. كرر للمجموعات الأخرى

#### ب. إضافة فئات الطلاب
1. اضغط على أيقونة المستخدم
2. اذهب إلى قسم "فئات الطلاب"
3. اضغط "إضافة فئة جديدة"
4. أدخل اسم الفئة واللون
5. كرر للفئات الأخرى

#### ج. إضافة الطلاب
**الطريقة الأولى: استيراد من Excel**
1. اذهب إلى الصفحة الرئيسية
2. اضغط "استيراد من Excel"
3. اختر ملف Excel
4. راجع البيانات
5. اضغط "استيراد"

**الطريقة الثانية: إضافة يدوية**
1. اضغط "إضافة طالب"
2. أدخل البيانات
3. اضغط "إضافة"

#### د. إضافة المعلمين (اختياري)
1. اذهب إلى "المعلمين"
2. اضغط "إضافة معلم"
3. أدخل البيانات وحدد المجموعات
4. اضغط "إضافة"

---

## 💾 استعادة نسخة احتياطية

إذا قمت بعمل نسخة احتياطية وتريد استعادتها:

### خطوات الاستعادة:
1. افتح النظام
2. سجّل الدخول (admin/admin)
3. اضغط على أيقونة المستخدم
4. **مهم**: مرر إلى أسفل الصفحة حتى تجد قسم "استيراد/تصدير البيانات"
5. اضغط على "استيراد البيانات الكاملة"
6. اختر ملف النسخة الاحتياطية (JSON)
7. انتظر حتى يتم الاستيراد
8. سيتم إعادة تحميل الصفحة تلقائياً

---

## 🔒 نصائح الأمان

### ✅ نصائح عامة
- 📌 غيّر بيانات الدخول فوراً بعد التصفير
- 📌 لا تشارك ملف `setup.html` مع أشخاص غير موثوقين
- 📌 احذف أو أخفِ الملف في بيئة الإنتاج
- 📌 اعمل نسخ احتياطية دورية (أسبوعية على الأقل)
- 📌 احفظ النسخ الاحتياطية في أماكن متعددة
- 📌 استخدم كلمات مرور قوية

### ⚠️ تحذيرات أمنية
- 🔴 لا تفتح `setup.html` إلا عند الحاجة الفعلية
- 🔴 لا تستخدم admin/admin إلا للدخول الأول فقط
- 🔴 لا تعطِ ملف Setup لأي شخص غير مسؤول النظام

---

## 📞 الدعم الفني

إذا واجهت أي مشاكل أو كان لديك أسئلة:

**المطور:** الأستاذ وائل الفيفي
**الجوال:** 0558890902
**واتساب:** [اضغط هنا للتواصل](https://wa.me/966558890902)

---

## ✅ قائمة مراجعة سريعة

### قبل التصفير:
- [ ] عملت نسخة احتياطية من البيانات
- [ ] أغلقت جميع نوافذ النظام الأخرى
- [ ] قرأت جميع التحذيرات
- [ ] فهمت أن العملية لا يمكن التراجع عنها
- [ ] حفظت ملف النسخة الاحتياطية في مكان آمن

### بعد التصفير:
- [ ] سجلت الدخول بـ admin/admin
- [ ] غيّرت بيانات الدخول فوراً
- [ ] أدخلت معلومات النظام (اسم المدرسة، إلخ)
- [ ] أضفت المجموعات
- [ ] أضفت فئات الطلاب
- [ ] أضفت الطلاب (استيراد أو يدوياً)

---

## 🎓 حالات الاستخدام

استخدم ملف `setup.html` فقط في هذه الحالات:

✅ **بداية استخدام النظام لأول مرة**
- عند تثبيت النظام لأول مرة
- لإزالة البيانات التجريبية

✅ **الحاجة لإعادة تهيئة كاملة**
- عند بداية عام دراسي جديد
- عند الانتقال إلى مدرسة أخرى

✅ **حل مشاكل قاعدة البيانات**
- عند وجود أخطاء في البيانات لا يمكن إصلاحها
- عند فساد قاعدة البيانات

❌ **لا تستخدمه في:**
- حذف طالب واحد (استخدم خاصية الحذف العادية)
- تعديل بيانات (استخدم خاصية التعديل)
- إزالة مجموعة (استخدم إدارة المجموعات)

---

## 🌟 نصيحة ذهبية

> **"النسخة الاحتياطية هي الطوق الوحيد للنجاة!"**

لا تصفّر قاعدة البيانات أبداً بدون عمل نسخة احتياطية، حتى لو كنت متأكداً أنك لا تحتاج البيانات!

---

**آخر تحديث:** ديسمبر 2025
**الإصدار:** 1.0.0
